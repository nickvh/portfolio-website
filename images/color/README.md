# Color Photos

Place your color photos in this folder.

## Current Photos

This folder contains your color photography collection.

## To Update

1. Add new photos to this folder
2. Run: `python3 generate_image_list.py` (from project root)
3. Or run: `make build` (from project root)

The script will automatically update `gallery-color.js` with the new image list.
